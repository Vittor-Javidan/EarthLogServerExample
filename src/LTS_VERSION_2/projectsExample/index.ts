import { ProjectDTO } from "../Types/DTO.js";
import { EMPTY } from "./EMPTY.js";
import { GEOLOGY } from "./GEOLOGY.js";

export const projectsExample: Record<string, ProjectDTO> = {
  'empty': EMPTY,
  'geology': GEOLOGY,
}
